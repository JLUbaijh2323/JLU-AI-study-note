
让我们开始。这份笔记**假设你已经掌握了所有基础公式**，它只专注于**“高级技巧”、“精华思维”、和“系统性SOP”**，目标是让你在数学竞赛中处理复杂题目时游刃有余。

---

### 🚀 数学竞赛 · 精华拔高思维笔记 (终极版)

#### 第一部分：高等数学 · “工具箱”与“构造论”

**核心思维：** 竞赛题不是在考你“会不会算”，而是考你“**会不会变形**”和“**会不会构造**”。

---

### 模块一：极限 (核心：泰勒的“降维打击”)

**思维：** 99%的 $x \to 0$ 且 $\frac{0}{0}$ 型的压轴题，本质都是泰勒展开。洛必达是“一阶泰勒”，而难题就是逼你展开到更高阶。

1. **“定阶”问题** (识别：$\lim \frac{f(x)}{x^n} = c \ne 0$，求 $n, c$)
    
    - **SOP (Standard Operating Procedure)：**
        
        1. 对分子 $f(x)$ 在 $x=0$ 处进行泰勒展开。
            
        2. 找出**第一个非零项** $C_k x^k$。
            
        3. **结论：** $n=k, c=C_k$。
            
    - **案例 (`A-15`):** $\lim \frac{2\arctan x - \ln\frac{1+x}{1-x}}{x^n}=c$
        
        - **思维：** $2\arctan x = 2(x - x^3/3 + \dots)$。 $\ln(1+x)-\ln(1-x) = 2(x + x^3/3 + \dots)$。
            
        - 分子 $=(2x - 2x^3/3) - (2x + 2x^3/3) = -4x^3/3 + \dots$
            
        - **结论：** $n=3, c=-4/3$。
            
2. **“抽象复合函数”极限** (识别：$\lim \frac{f(x)-f(g(x))}{x^k}$)
    
    - **SOP：**
        
        1. **外层展开：** $f(u) = f(0) + f'(0)u + \frac{f''(0)}{2!}u^2 + \dots$
            
        2. **内芯展开：** $g(x) = a_0 + a_1 x + a_2 x^2 + \dots$
            
        3. **“代入”与“合并”：** 将 $g(x)$ 的展开式**代入** $u$ 中，**合并** $x$ 的同次幂，只保留到 $x^k$ 阶。
            
    - **案例 (`D-3`):** $f'(0)=0, f''(0)=4$，求 $\lim \frac{f(x)-f[\ln(1+x)]}{x^3}$
        
        - **思维：** $f(x) = f(0) + 2x^2 + o(x^3)$。
            
        - 内芯 $u = \ln(1+x) = x - x^2/2 + o(x^3)$。
            
        - $f(u) = f(0) + 2u^2 + \dots = f(0) + 2(x - x^2/2)^2 + o(x^3) = f(0) + 2(x^2 - x^3) + o(x^3)$。
            
        - 分子 $= (f(0)+2x^2) - (f(0)+2x^2-2x^3) = 2x^3$。
            
        - **结论：** $\lim \frac{2x^3}{x^3} = 2$。
            
3. **$1^\infty$ 型极限** (识别：$\lim [f(x)]^{g(x)}$)
    
    - **SOP：**
        
        1. **标准形式：** $\exp\left[\lim g(x) \ln f(x)\right]$。
            
        2. **关键一步：** $\ln f(x) = \ln(1 + [f(x)-1])$。
            
        3. **泰勒降维：** $f(x) \to 1$ 时，$\ln(1 + [f(x)-1]) \sim f(x)-1$。
            
        4. 此时问题转化为求 $\lim g(x) \cdot (f(x)-1)$。
            
    - **案例 (`F-15`):** $\lim_{x\to\infty} (x \arctan(1/x))^{x^2}$
        
        - **思维：** (1) $e^{\lim x^2 \ln(x \arctan(1/x))}$。
            
        - (2) (令 $t=1/x$) $\to e^{\lim_{t\to 0} \frac{\ln(\frac{\arctan t}{t})}{t^2}}$。
            
        - (3) (泰勒) $\frac{\arctan t}{t} = 1 - t^2/3 + o(t^3)$。
            
        - (4) (泰勒) $\ln(1 - t^2/3 + \dots) \sim -t^2/3$。
            
        - **结论：** $e^{-1/3}$。
            

---

### 模块二：中值定理 (压轴题的“构造”艺术)

**核心思维：** 90%的 $\exists \xi$ 证明都是“**Rolle 定理的伪装**”。你的**唯一**目标就是构造一个 $F(x)$，使其满足 $F(a) = F(b)$。

1. **类型一：证明 $f'(\xi) + P(\xi)f(\xi) = 0$** (如 $f'(\xi)=f(\xi)$)
    
    - **SOP (微分方程积分因子法)：**
        
        1. **识别：** $y' + P(x)y = 0$。
            
        2. **构造：$F(x) = f(x) \cdot e^{\int P(x)dx}$**。
            
        3. **验证：** $F'(x) = e^{\int P(x)dx} [f'(x) + P(x)f(x)]$。
            
        4. **目标：** 利用题设条件 (尤其是含积分的条件)，通过**积分中值定理**，证明 $\exists a, b$ 使 $F(a)=F(b)$。
            
        5. (Rolle) $\implies \exists \xi$ 使 $F'(\xi)=0 \implies [f'(\xi) + P(\xi)f(\xi)]=0$。
            
    - **案例 (`MVT-J-6`):** $f(1)=3\int_{0}^{1/3}e^{1-x}f(x)dx$，证 $f'(\xi)=f(\xi)$。
        
        - **思维：** $y'-y=0 \implies P(x)=-1 \implies$ **构造 $F(x) = e^{-x}f(x)$**。
            
        - $F(1) = e^{-1}f(1) = e^{-1} [3\int_0^{1/3} e^{1-x}f(x)dx] = 3\int_0^{1/3} e^{-x}f(x)dx = 3\int_0^{1/3} F(x)dx$。
            
        - (积分中值) $\implies F(1) = 3 \cdot F(c) \cdot (1/3) = F(c)$， $c \in (0, 1/3)$。
            
        - (Rolle) $F(1)=F(c) \implies \exists \xi \in (c, 1)$ 使 $F'(\xi)=0$。**证毕**。
            
2. **类型二：证明 $f''(\xi) = 0$**
    
    - **SOP (Rolle-on-Rolle)：**
        
        1. **目标：** 在 $[a, b]$ 上对 $f'(x)$ 用Rolle。
            
        2. **你需要：** 找到两个点 $c_1, c_2$ 使得 $f'(c_1) = f'(c_2)$ (通常都=0)。
            
    - **案例 (`MVT-J-1`):** $f(0)=f(1)=\int_0^1 f(x)dx$。
        
        - **思维：** (1) $f(0)=f(1) \implies$ (Rolle) $\exists c_1$ 使 $f'(c_1)=0$。
            
        - (2) $f(1) = \int_0^1 f(x)dx \implies$ (积分中值) $\exists c_2$ 使 $f(c_2) = f(1)$。
            
        - (3) (Rolle) $f(c_2)=f(1) \implies \exists c_3$ 使 $f'(c_3)=0$。
            
        - (4) (Rolle on $f'$) $f'(c_1)=f'(c_3)=0 \implies \exists \xi$ 使 $f''(\xi)=0$。
            
3. **类型三：证明 $\exists \xi, \eta$ (两个点)**
    
    - **SOP：(分段使用Lagrange或Cauchy)**
        
    - **案例 (`MVT-J-15`):** $f(0)=0, f(1)=1$，证 $\frac{2}{f'(\xi)} + \frac{1}{f'(\eta)} = 3$`
        
        - **思维：** (1) 识别 $f'$ 在分母 $\implies$ 想到 $\frac{1}{f'(\xi)} = \frac{c-a}{f(c)-f(a)}$。
            
        - (2) 缺少“中间点”。Part(1)提示 $\exists c, f(c)=2/3$。
            
        - (3) **分段Lagrange**：
            
            - $[0, c]$: $f'(\xi) = \frac{f(c)-f(0)}{c-0} = \frac{2/3}{c} \implies \frac{1}{f'(\xi)} = \frac{3c}{2}$。
                
            - $[c, 1]$: $f'(\eta) = \frac{f(1)-f(c)}{1-c} = \frac{1-2/3}{1-c} = \frac{1/3}{1-c} \implies \frac{1}{f'(\eta)} = 3(1-c)$。
                
        - (4) **代入：** $\frac{2}{f'(\xi)} + \frac{1}{f'(\eta)} = 2(\frac{3c}{2}) + 3(1-c) = 3c + 3 - 3c = 3$。
            

---

### 模块三：一元积分学 (高级工具与证明)

**核心思维：** 竞赛积分=“**变形**”+“**三大不等式**”+“**$\Gamma$函数**”。

1. **高级计算工具 (SOP)**
    
    - **$\Gamma$ 函数 (Gamma Function)：**
        
        - **识别：** $\int_0^\infty x^n e^{-kx} dx$。
            
        - **SOP (必背)：** $\int_0^\infty x^n e^{-kx} dx = \frac{\Gamma(n+1)}{k^{n+1}} = \frac{n!}{k^{n+1}}$
            
        - **应用 (`I-J-40`):** $\int_0^\infty x^2 e^{-2x} dx \implies n=2, k=2 \implies \frac{2!}{2^3} = 1/4$。
            
    - **分部积分表格法 (Tabular Integration)：**
        
        - **适用：** $\int P(x) \cdot f(x) dx$，其中 $P(x)$ 是多项式，$f(x)$ 是 $e^{kx}, \sin kx, \cos kx$。
            
        - **SOP：**
            
            1. 列 `D` (求导 $P(x) \to 0$)，列 `I` (积分 $f(x)$)。
                
            2. **答案 = $\sum$ [ (D列第 $k$ 行) $\times$ (I列第 $k+1$ 行) $\times (-1)^{k-1}$ ]** (注意正负号 `+-+-...`)
                
    - **$e^x [f(x)+f'(x)]$ 型：**
        
        - **识别 (`I-J-6(1)`):** $\int \frac{x e^x}{(x+1)^2} dx$。
            
        - **SOP：** 立即将被积函数（非 $e^x$ 部分）拆解。
            
        - $\frac{x}{(x+1)^2} = \frac{(x+1)-1}{(x+1)^2} = \frac{1}{x+1} - \frac{1}{(x+1)^2}$。
            
        - $f(x) = \frac{1}{x+1}, f'(x) = -\frac{1}{(x+1)^2}$。
            
        - **结论：** 积分为 $e^x f(x) = \frac{e^x}{x+1} + C$。
            
2. **积分不等式 (三大神器)**
    
    - **神器一：柯西-施瓦茨不等式 (Cauchy-Schwarz)**
        
        - **公式：** $[\int_a^b f(x)g(x) dx]^2 \le \int_a^b f^2(x) dx \cdot \int_a^b g^2(x) dx$
            
        - **识别信号：** 证明中同时出现**“平方的积分”**和**“积分的平方”**。
            
        - **案例 (`I-J-32`):** $f(1)-f(0)=1$，证 $\int_0^1 [f'(x)]^2 dx \ge 1$。
            
        - **思维 (“1”的技巧)：** $1^2 = [\int_0^1 f'(x) dx]^2 = [\int_0^1 f'(x) \cdot 1 dx]^2 \le (\int_0^1 [f']^2 dx) \cdot (\int_0^1 1^2 dx) = \int_0^1 [f']^2 dx$。
            
    - **神器二：切比雪夫积分不等式 (Chebyshev)**
        
        - **公式：** $f, g$ **同向** (同增/同减) $\implies \frac{1}{b-a}\int_a^b fg dx \ge (\frac{1}{b-a}\int f dx) (\frac{1}{b-a}\int g dx)$。
            
        - **识别信号：** 题目只给了 $f, g$ 的**单调性**。
            
        - **案例 (`I-J-26`):** $f(x)$ 递增，证 $\int_a^b xf(x)dx \ge \frac{a+b}{2} \int_a^b f(x)dx$。
            
        - **思维：** 移项 $\int_a^b (x - \frac{a+b}{2}) f(x) dx \ge 0$。 $g(x)=x-\frac{a+b}{2}$ 和 $f(x)$ 都是**增**函数。
            
        - 套用(同向)公式： $\int_a^b g(x)dx = \int_a^b (x - \frac{a+b}{2}) dx = 0$。
            
        - $\implies \int_a^b g(x)f(x) dx \ge \frac{1}{b-a}(\int g)(\int f) = 0$。**证毕**。
            
    - **神器三：琴生不等式 (Jensen's Inequality)**
        
        - **公式：** $f(x)$ 是**凸函数** ($f''>0$) $\implies f(E[X]) \le E[f(X)]$。
            
        - **积分形式 (`MVT-J-25`):** $f(\int_a^b x g(x) dx) \le \int_a^b f(x) g(x) dx$ (需 $g(x) \ge 0, \int_a^b g(x) dx = 1$)。
            
        - **离散形式 (考点)：** $f(\sum k_i x_i) \le \sum k_i f(x_i)$ (需 $\sum k_i = 1$)。
            
        - **更简单形式 ($k_i = 1/n$)：** $f(\frac{x_1+\dots+x_n}{n}) \le \frac{f(x_1)+\dots+f(x_n)}{n}$。
            
3. **定积分对称性 (SOP)**
    
    - **SOP 1 (区间再现)：** $\int_0^{\pi/2} \frac{f(\sin x)}{f(\sin x)+f(\cos x)} dx$
        
        - **动作：** 令 $t = \pi/2 - x$，得到 $I = \int_0^{\pi/2} \frac{f(\cos t)}{f(\cos t)+f(\sin t)} dt$。
            
        - $I+I = \int_0^{\pi/2} 1 dx = \pi/2 \implies I = \pi/4$。
            
    - **SOP 2 (华里士公式)：** $\int_0^{\pi/2} \sin^n x dx = \int_0^{\pi/2} \cos^n x dx = \begin{cases} \frac{(n-1)!!}{n!!} \frac{\pi}{2} & n \text{为偶} \\ \frac{(n-1)!!}{n!!} & n \text{为奇} \end{cases}$。
        
    - **SOP 3 (区间 $[0, \pi]$)：** $\int_0^\pi x f(\sin x) dx = \frac{\pi}{2} \int_0^\pi f(\sin x) dx = \pi \int_0^{\pi/2} f(\sin x) dx$。
        

---

### 模块四：无穷级数 (微积分的终局)

**核心思维：** 级数是“广义”的微积分。敛散性是“抽象极限”，求和是“抽象积分”。

1. **Abel 定理 (收敛域的核心)**
    
    - **SOP：**
        
        1. $\sum a_n (x-x_0)^n$ 在 $x_1$ 处**收敛** $\implies$ 在 $|x-x_0| < |x_1-x_0|$ 内**绝对收敛**。
            
        2. $\sum a_n (x-x_0)^n$ 在 $x_2$ 处**发散** $\implies$ 在 $|x-x_0| > |x_2-x_0|$ 处**发散**。
            
        3. **“条件收敛”** 只能发生在**端点**上。
            
    - **案例 (`S-F-6`):** $\sum a_n(2x-1)^n$ 在 $x=-1$ 条件收敛，求 $\sum a_n x^{2n}$ 的 $R$。
        
        - **思维：** (1) $y=2x-1$，中心 $x_0=1/2$。
            
        - (2) $x=-1$ 是端点 $\implies R_y = |-1 - 1/2| = 3/2$。
            
        - (3) $\sum a_n y^n$ 的 $R_y = 3/2$。
            
        - (4) $\sum a_n x^{2n}$ $\to$ 令 $z=x^2$ $\to \sum a_n z^n$。
            
        - (5) $R_z = R_y = 3/2 \implies |z| < 3/2 \implies |x^2| < 3/2 \implies R_x = \sqrt{3/2}$。
            
2. **函数 $\leftrightarrow$ 级数 (求和与展开)**
    
    - **SOP 1 (反求导数)：** $f^{(n)}(a)$ $\implies$ **立即**在 $x=a$ 处泰勒展开 $f(x)$，对比 $\frac{f^{(n)}(a)}{n!}$。
        
    - **SOP 2 (凑 $e^x$)：** 看到 $\frac{1}{n!}$ $\implies$ 凑 $e^x = \sum \frac{x^n}{n!}$。
        
        - **技巧：** $n^2 = n(n-1)+n$ $\to$ $\frac{n^2}{n!} = \frac{1}{(n-2)!} + \frac{1}{(n-1)!}$。
            
    - **SOP 3 (逐项积分/微分)：** 看到 $\frac{1}{n}$ 或 $n$ $\implies$ **先求导/积分**，变为 $\sum x^n = \frac{1}{1-x}$，**再积/导回去**，最后**代入 $x=0$ 定常数 $C$**。
        
    - **SOP 4 (凑 $\cosh / \cos$)：** 看到“稀疏项” (如 $\frac{x^{4n}}{(4n)!}$)
        
        - **思维：** $\frac{1}{2}(\cosh x + \cos x) = \sum_{n=0}^\infty \frac{x^{4n}}{(4n)!}$。
            
        - $\frac{1}{2}(\sinh x + \sin x) = \sum_{n=0}^\infty \frac{x^{4n+1}}{(4n+1)!}$。
            
3. **傅里叶级数 (Dirichlet 定理)**
    
    - **SOP：**
        
        1. **查奇偶：** $f(x)$ 是偶 $\implies b_n=0$； $f(x)$ 是奇 $\implies a_n=0$。
            
        2. **求系数：** $a_n = \frac{2}{L} \int_0^L \dots$ (必用分部积分)。
            
        3. **求 $\sum \frac{1}{n^2}$ (反求)：**
            
            - 写出 $f(x) = \frac{a_0}{2} + \sum \dots$
                
            - **代入 $x$ 值：** 选一个**连续点** $x_0$ (通常是 $x=0$ 或 $x=L$)。
                
            - $f(x_0) = S(x_0)$，解出级数和。
                
            - **S-T-S 技巧：** $S_{\text{all}} = S_{\text{odd}} + S_{\text{even}}$。
                
            - $S_{\text{even}} = \sum \frac{1}{(2k)^2} = \frac{1}{4} \sum \frac{1}{k^2} = \frac{1}{4} S_{\text{all}}$。
                
            - $S_{\text{all}} = S_{\text{odd}} + \frac{1}{4} S_{\text{all}} \implies S_{\text{all}} = \frac{4}{3} S_{\text{odd}}$。
                
            - (如 `S-J-34` 中 $S_{\text{odd}} = \pi^2/8 \implies S_{\text{all}} = \frac{4}{3}(\frac{\pi^2}{8}) = \frac{\pi^2}{6}$)
                

---

### 模块五：线面积分 (G-G-S 公式与“挖洞”)

**核心思维：** 高斯(Gauss)、格林(Green)、斯托克斯(Stokes)的本质是“**降维**”：体 $\to$ 面，面 $\to$ 线。

1. **Green/Gauss/Stokes 的决策SOP：**
    
    1. **识别：** 拿到一个**曲线/曲面积分** $I$。
        
    2. **计算“旋度”/“散度”：**
        
        - **Green:** 计算 $\frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y}$。
            
        - **Gauss:** 计算 $\text{div}(\vec{F}) = \frac{\partial P}{\partial x} + \frac{\partial Q}{\partial y} + \frac{\partial R}{\partial z}$。
            
        - **Stokes:** 计算 $\text{rot}(\vec{F})$。
            
    3. **决策：**
        
        - 如果“旋度”/“散度”的计算结果**等于 0 或一个常数** (如 `S-T-15` $\text{rot}(\vec{F}) \cdot \vec{n} = 2$) $\implies$ **立即使用G-G-S公式！** 积分会变成 `常数 $\times$ 面积/体积`。
            
        - 如果结果**不为0或常数** $\implies$ G-G-S 仍然是首选，你需要硬算降维后的 $\iint$ 或 $\iiint$。
            
2. **“挖洞”技巧 (处理奇异点)**
    
    - **识别 (`S-T-7`):** $\oint_L \frac{P dx + Q dy}{x^2+4y^2}$。 $P, Q$ 在 $(0,0)$ 奇异。
        
    - **SOP：**
        
        1. **验证保守场：** 计算 $\frac{\partial Q}{\partial x} = \frac{\partial P}{\partial y}$ (在 $D \setminus \{(0,0)\}$ 区域内)。
            
        2. **“挖洞”：** $L$ 围绕着 $(0,0)$。在 $L$ 内部挖一个**同向**的**小闭路 $L_\epsilon$**（通常是小圆或小椭圆）。
            
        3. **连通：** 用两条路径连接 $L$ 和 $L_\epsilon$，构造一个**不包含** $(0,0)$ 的单连通闭区域。
            
        4. **应用Green：** $\oint_{L} + \oint_{L_\epsilon (\text{反向})} = \iint_D (\dots) = 0$。
            
        5. **结论：** $\oint_L (\dots) = \oint_{L_\epsilon (\text{正向})} (\dots)$。
            
    - **技巧：** $L_\epsilon$ 的形状**必须**和分母的形状匹配。
        
        - 分母 $x^2+y^2 \implies$ 挖**圆** $x^2+y^2=\epsilon^2$。
            
        - 分母 $x^2+4y^2 \implies$ 挖**椭圆** $x^2+4y^2=\epsilon^2$。
            
        - **参数化** $L_\epsilon$ (如 $x=\epsilon\cos t, y=\frac{\epsilon}{2}\sin t$)，代入原积分，轻松算出 $I=\pi$。
            

---

### 模块六：多元积分 (对称性与坐标系)

1. **二重/三重积分的“对称性”SOP**
    
    - **奇偶对称性：**
        
        - **识别：** 积分区域 $D$ (如 $x^2+y^2 \le 1$) **关于 $y$ 轴对称** (即 $x \leftrightarrow -x$)。
            
        - **SOP：** 立即检查被积函数 $f(x, y)$ **关于 $x$ 的奇偶性**。
            
        - $f(-x, y) = -f(x, y)$ (对 $x$ 是奇) $\implies \iint_D f(x, y) d\sigma = 0$。
            
        - $f(-x, y) = f(x, y)$ (对 $x$ 是偶) $\implies \iint_D f(x, y) d\sigma = 2 \iint_{D_1} f(x, y) d\sigma$ ($D_1$ 是 $x \ge 0$ 的一半)。
            
    - **轮换对称性 (竞赛常用)：**
        
        - **识别：** 积分区域 $D$ **关于 $y=x$ 对称**。
            
        - **SOP：** $\iint_D f(x, y) d\sigma = \iint_D f(y, x) d\sigma$。
            
        - **应用：** $I = \iint_D f(x, y) d\sigma \implies 2I = \iint_D [f(x, y) + f(y, x)] d\sigma$。
            
        - **案例：** $\iint_D \frac{f(x)}{f(x)+f(y)} d\sigma$ $\implies 2I = \iint_D \frac{f(x)+f(y)}{f(x)+f(y)} d\sigma = \iint_D 1 d\sigma = \text{Area}(D)$。
            
2. **高难度“交换次序”**
    
    - **识别 (`M-J-18`):** $\int_0^a dx \int_0^x \frac{f'(y)}{\sqrt{(a-x)(x-y)}} dy$
        
    - **SOP：**
        
        1. 画出积分域 $D$: $0 \le y \le x \le a$ (三角形)。
            
        2. **交换次序：** $\int_0^a f'(y) dy \int_y^a \frac{1}{\sqrt{(a-x)(x-y)}} dx$。
            
        3. **处理内层 (关键)：** $I_{内} = \int_y^a \frac{dx}{\sqrt{-(x^2 - (a+y)x + ay)}}$。
            
        4. **“标准换元” (必背)：** $\int_a^b \frac{dx}{\sqrt{(b-x)(x-a)}}$ 型。
            
        5. 令 $x = a \sin^2\theta + y \cos^2\theta$ (或 $x = \frac{a+y}{2} + \frac{a-y}{2} \sin t$)。
            
        6. **结论 (必背)：** $I_{内} = \pi$。
            
        7. **代回：** $\int_0^a f'(y) \cdot \pi dy = \pi [f(y)]_0^a = \pi(f(a)-f(0))$。
            

---

### 模块七：线性代数 (结构与反求)

1. **$r(A^*)$ 与 $r(A)$ 的关系 (必背)**
    
    - 若 $r(A) = n \implies r(A^*) = n$
        
    - 若 $r(A) = n-1 \implies r(A^*) = 1$
        
    - 若 $r(A) < n-1 \implies r(A^*) = 0$
        
2. **$r(A)=1$ 矩阵 (核心)**
    
    - **充要条件：** $A = \alpha \beta^T$ (列向量 $\alpha \times$ 行向量 $\beta^T$)
        
    - **性质：** $A^2 = \text{tr}(A) \cdot A$
        
    - **特征值：** $\lambda_1 = \text{tr}(A)$，其余 $\lambda_i = 0$。
        
3. **抽象特征值 (基变换)**
    
    - **识别：** $A\alpha_1 = k_1\alpha_1 + k_2\alpha_2$, $A\alpha_2 = \dots$
        
    - **SOP：**
        
        1. **“打包”**： $A(\alpha_1, \alpha_2) = (k_1\alpha_1 + k_2\alpha_2, \dots)$
            
        2. **“提公因式”：** $A(\alpha_1, \alpha_2) = (\alpha_1, \alpha_2) \begin{pmatrix} k_1 & \dots \\ k_2 & \dots \end{pmatrix}$
            
        3. **“翻译”：** $AP = PB$
            
        4. **“相似”：** $A = PBP^{-1} \implies A \sim B$。
            
        5. **结论：$A$ 和 $B$ 有相同的特征值**。求 $\det(B - \lambda E) = 0$ 即可。
            
4. **反求对称矩阵 $A$ (压轴题)**
    
    - **SOP：**
        
        1. 用尽所有条件 (秩、迹、$\lambda_i, \xi_i$) 求出所有**特征值 $\Lambda$** 和**特征向量 $P$**。
            
        2. **核心：** 利用 $A^T=A$ $\implies$ **不同特征值对应的特征向量必须正交** ($\xi_i^T \xi_j = 0$)。用此条件解出未知参数。
            
        3. **单位化：** 将 $P$ 的所有列向量单位化，得到**正交矩阵 $Q$**。
            
        4. **重建 $A$：** $A = Q \Lambda Q^T$。
---

### 🚀 数学竞赛 · 终极拔高思维笔记 (微积分)

#### 模块一：极限 (核心：泰勒的“降维打击”)

**核心思维：** 99%的 $x \to 0$ 且 $\frac{0}{0}$ 型的压轴题，本质都是泰勒展开。洛必达是“一阶泰勒”，而难题就是逼你展开到更高阶。

1. **“定阶”问题** (识别：$\lim \frac{f(x)}{x^n} = c \ne 0$，求 $n, c$)
    
    - **SOP (Standard Operating Procedure)：**
        
        1. 对分子 $f(x)$ 在 $x=0$ 处进行泰勒展开。
            
        2. 找出**第一个非零项** $C_k x^k$。
            
        3. **结论：** $n=k, c=C_k$。
            
    - **案例 (`A-15`):** $\lim \frac{2\arctan x - \ln\frac{1+x}{1-x}}{x^n}=c$
        
        - **思维：** $2\arctan x = 2(x - x^3/3 + \dots)$。 $\ln(1+x)-\ln(1-x) = 2(x + x^3/3 + \dots)$。
            
        - 分子 $=(2x - 2x^3/3) - (2x + 2x^3/3) = -4x^3/3 + \dots$
            
        - **结论：** $n=3, c=-4/3$。
            
2. **“抽象复合函数”极限** (识别：$\lim \frac{f(x)-f(g(x))}{x^k}$)
    
    - **SOP：**
        
        1. **外层展开：** $f(u) = f(0) + f'(0)u + \frac{f''(0)}{2!}u^2 + \dots$
            
        2. **内芯展开：** $g(x) = a_0 + a_1 x + a_2 x^2 + \dots$
            
        3. **“代入”与“合并”：** 将 $g(x)$ 的展开式**代入** $u$ 中，**合并** $x$ 的同次幂，只保留到 $x^k$ 阶。
            
    - **案例 (`D-3`):** $f'(0)=0, f''(0)=4$，求 $\lim \frac{f(x)-f[\ln(1+x)]}{x^3}$
        
        - **思维：** $f(x) = f(0) + 2x^2 + o(x^3)$。
            
        - 内芯 $u = \ln(1+x) = x - x^2/2 + o(x^3)$。
            
        - $f(u) = f(0) + 2u^2 + \dots = f(0) + 2(x - x^2/2)^2 + o(x^3) = f(0) + 2(x^2 - x^3) + o(x^3)$。
            
        - 分子 $= (f(0)+2x^2) - (f(0)+2x^2-2x^3) = 2x^3$。
            
        - **结论：** $\lim \frac{2x^3}{x^3} = 2$。
            
3. **$1^\infty$ 型极限** (识别：$\lim [f(x)]^{g(x)}$)
    
    - **SOP：**
        
        1. **标准形式：** $\exp\left[\lim g(x) \ln f(x)\right]$。
            
        2. **关键一步：** $\ln f(x) = \ln(1 + [f(x)-1])$。
            
        3. **泰勒降维：** $f(x) \to 1$ 时，$\ln(1 + [f(x)-1]) \sim f(x)-1$。
            
        4. 此时问题转化为求 $\lim g(x) \cdot (f(x)-1)$。
            
    - **案例 (`F-15`):** $\lim_{x\to\infty} (x \arctan(1/x))^{x^2}$
        
        - **思维：** (1) $e^{\lim x^2 \ln(x \arctan(1/x))}$。
            
        - (2) (令 $t=1/x$) $\to e^{\lim_{t\to 0} \frac{\ln(\frac{\arctan t}{t})}{t^2}}$。
            
        - (3) (泰勒) $\frac{\arctan t}{t} = 1 - t^2/3 + o(t^3)$。
            
        - (4) (泰勒) $\ln(1 - t^2/3 + \dots) \sim -t^2/3$。
            
        - **结论：** $e^{-1/3}$。
            

---

##### 模块二：导数与积分 (高级工具)

1. **高阶导数 (SOP)**
    
    - **SOP 1 (分解法)：** 适用于“有理函数”。
        
        - **动作：** **部分分式** $\to$ 套用 $(\frac{1}{x-a})^{(n)} = \frac{(-1)^n n!}{(x-a)^{n+1}}$。
            
    - **SOP 2 (莱布尼茨公式)：** $(uv)^{(n)} = \sum_{k=0}^n C_n^k u^{(k)} v^{(n-k)}$。
        
        - **适用：** **必须**是 “$x^k$ (多项式) $\times$ $f(x)$ (易求导)”。
            
    - **SOP 3 (记忆)：** $(\sin kx)^{(n)} = k^n \sin(kx + \frac{n\pi}{2})$；$(\cos kx)^{(n)} = k^n \cos(kx + \frac{n\pi}{2})$。
        
2. **分部积分表格法 (Tabular Integration)**
    
    - **适用：** $\int P(x) \cdot f(x) dx$，其中 $P(x)$ 是多项式，$f(x)$ 是 $e^{kx}, \sin kx, \cos kx$。
        
    - **SOP：**
        
        1. 列 `D` (求导 $P(x) \to 0$)，列 `I` (积分 $f(x)$)。
            
        2. **答案 = $\sum$ [ (D列第 $k$ 行) $\times$ (I列第 $k+1$ 行) $\times (-1)^{k-1}$ ]** (注意正负号 `+-+-...`)
            
3. **$\Gamma$ 函数 (Gamma Function)**
    
    - **核心：** 阶乘的实数推广 $\Gamma(z) = \int_0^\infty t^{z-1} e^{-t} dt$。
        
    - **性质：** $\Gamma(n+1) = n!$ (n为正整数)； $\Gamma(1/2) = \sqrt{\pi}$。
        
    - **权威SOP (必背)：** $\int_0^\infty x^n e^{-kx} dx = \frac{\Gamma(n+1)}{k^{n+1}} = \frac{n!}{k^{n+1}}$
        
    - **应用 (`I-J-40`):** $\int_0^\infty x^2 e^{-2x} dx \implies n=2, k=2 \implies \frac{2!}{2^3} = 1/4$。 (10秒钟降维打击分部积分)
        
4. **变限积分求导 (Leibniz Rule)**
    
    - **识别：** $x$ 同时出现在**积分限**和**被积函数**中。
        
    - **SOP 1 (推荐)：** 尽量通过**换元** (如 $u=x-t$ 或 $u=xt$) 把 $x$ 提出来，变成 $\varphi(x) = g(x) \int_{a(x)}^{b(x)} h(t) dt$ 的形式，再用乘法法则求导。
        
    - **SOP 2 (强攻)：** $\frac{d}{dx} \left[ \int_{a(x)}^{b(x)} f(x, t) dt \right] = \underbrace{\int_{a(x)}^{b(x)} \frac{\partial f}{\partial x} dt}_{\text{对积分号内求导}} + \underbrace{f(x, b(x)) \cdot b'(x) - f(x, a(x)) \cdot a'(x)}_{\text{标准FTC}}$
        

---

##### 模块三：中值定理 (压轴题的“构造”艺术)

**核心思维：** 90%的 $\exists \xi$ 证明都是“**Rolle 定理的伪装**”。你的**唯一**目标就是构造一个 $F(x)$，使其满足 $F(a) = F(b)$。

1. **类型一：证明 $f'(\xi) + P(\xi)f(\xi) = 0$** (如 $f'(\xi)=f(\xi)$)
    
    - **SOP (微分方程积分因子法)：**
        
        1. **识别：** $y' + P(x)y = 0$。
            
        2. **构造：$F(x) = f(x) \cdot e^{\int P(x)dx}$**。
            
        3. **验证：** $F'(x) = e^{\int P(x)dx} [f'(x) + P(x)f(x)]$。
            
        4. **目标：** 利用题设条件 (尤其是含积分的条件)，通过**积分中值定理**，证明 $\exists a, b$ 使 $F(a)=F(b)$。
            
        5. (Rolle) $\implies \exists \xi$ 使 $F'(\xi)=0 \implies [f'(\xi) + P(\xi)f(\xi)]=0$。
            
    - **案例 (`MVT-J-6`):** $f(1)=3\int_{0}^{1/3}e^{1-x}f(x)dx$，证 $f'(\xi)=f(\xi)$。
        
        - **思维：** $y'-y=0 \implies P(x)=-1 \implies$ **构造 $F(x) = e^{-x}f(x)$**。
            
        - $F(1) = e^{-1}f(1) = e^{-1} [3\int_0^{1/3} e^{1-x}f(x)dx] = 3\int_0^{1/3} e^{-x}f(x)dx = 3\int_0^{1/3} F(x)dx$。
            
        - (积分中值) $\implies F(1) = 3 \cdot F(c) \cdot (1/3) = F(c)$， $c \in (0, 1/3)$。
            
        - (Rolle) $F(1)=F(c) \implies \exists \xi \in (c, 1)$ 使 $F'(\xi)=0$。**证毕**。
            
2. **类型二：证明 $f''(\xi) = 0$**
    
    - **SOP (Rolle-on-Rolle)：**
        
        1. **目标：** 在 $[a, b]$ 上对 $f'(x)$ 用Rolle。
            
        2. **你需要：** 找到两个点 $c_1, c_2$ 使得 $f'(c_1) = f'(c_2)$ (通常都=0)。
            
    - **案例 (`MVT-J-1`):** $f(0)=f(1)=\int_0^1 f(x)dx$。
        
        - **思维：** (1) $f(0)=f(1) \implies$ (Rolle) $\exists c_1$ 使 $f'(c_1)=0$。
            
        - (2) $f(1) = \int_0^1 f(x)dx \implies$ (积分中值) $\exists c_2$ 使 $f(c_2) = f(1)$。
            
        - (3) (Rolle) $f(c_2)=f(1) \implies \exists c_3$ 使 $f'(c_3)=0$。
            
        - (4) (Rolle on $f'$) $f'(c_1)=f'(c_3)=0 \implies \exists \xi$ 使 $f''(\xi)=0$。
            
3. **类型三：证明 $\exists \xi, \eta$ (两个点)**
    
    - **SOP：(分段使用Lagrange或Cauchy)**
        
    - **案例 (`MVT-J-15`):** $f(0)=0, f(1)=1$，证 $\frac{2}{f'(\xi)} + \frac{1}{f'(\eta)} = 3$`
        
        - **思维：** (1) 识别 $f'$ 在分母 $\implies$ 想到 $\frac{1}{f'(\xi)} = \frac{c-a}{f(c)-f(a)}$。
            
        - (2) 缺少“中间点”。Part(1)提示 $\exists c, f(c)=2/3$。
            
        - (3) **分段Lagrange**：
            
            - $[0, c]$: $f'(\xi) = \frac{f(c)-f(0)}{c-0} = \frac{2/3}{c} \implies \frac{1}{f'(\xi)} = \frac{3c}{2}$。
                
            - $[c, 1]$: $f'(\eta) = \frac{f(1)-f(c)}{1-c} = \frac{1-2/3}{1-c} = \frac{1/3}{1-c} \implies \frac{1}{f'(\eta)} = 3(1-c)$。
                
        - (4) **代入：** $\frac{2}{f'(\xi)} + \frac{1}{f'(\eta)} = 2(\frac{3c}{2}) + 3(1-c) = 3c + 3 - 3c = 3$。
            

---

##### 模块四：积分不等式 (三大神器)

1. **神器一：柯西-施瓦茨不等式 (Cauchy-Schwarz)**
    
    - **公式：** $[\int_a^b f(x)g(x) dx]^2 \le \int_a^b f^2(x) dx \cdot \int_a^b g^2(x) dx$
        
    - **识别信号：** 证明中同时出现**“平方的积分”**和**“积分的平方”**。
        
    - **案例 (`I-J-32`):** $f(1)-f(0)=1$，证 $\int_0^1 [f'(x)]^2 dx \ge 1$。
        
        - **思维 (“1”的技巧)：** $1^2 = [\int_0^1 f'(x) dx]^2 = [\int_0^1 f'(x) \cdot 1 dx]^2 \le (\int_0^1 [f']^2 dx) \cdot (\int_0^1 1^2 dx) = \int_0^1 [f']^2 dx$。
            
    - **证明 (SOP)：** 构造 $H(t) = \int_a^b [f(x) + t g(x)]^2 dx \ge 0$。这是一个关于 $t$ 的二次函数 $At^2+Bt+C \ge 0$，故其判别式 $\Delta = B^2-4AC \le 0$，展开即得。
        
2. **神器二：切比雪夫积分不等式 (Chebyshev)**
    
    - **公式：** $f, g$ **同向** (都增或都减) $\implies \frac{1}{b-a}\int_a^b fg dx \ge (\frac{1}{b-a}\int f dx) (\frac{1}{b-a}\int g dx)$。
        
    - **识别信号：** 题目只给了 $f, g$ 的**单调性**。
        
    - **案例 (`I-J-26`):** $f(x)$ 递增，证 $\int_a^b xf(x)dx \ge \frac{a+b}{2} \int_a^b f(x)dx$。
        
        - **思维：** 移项 $\int_a^b (x - \frac{a+b}{2}) f(x) dx \ge 0$。 $g(x)=x-\frac{a+b}{2}$ 和 $f(x)$ 都是**增**函数。
            
        - 套用(同向)公式： $\int_a^b g(x)dx = \int_a^b (x - \frac{a+b}{2}) dx = 0$。
            
        - $\implies \int_a^b g(x)f(x) dx \ge \frac{1}{b-a}(\int g)(\int f) = 0$。**证毕**。
            
    - **(SOP 证法 2)：** 构造 $H(x) = [g(x)-g(y)][f(x)-f(y)] \ge 0$，二重积分展开即得。
        
3. **神器三：积分的“对称性”与“区间变换”**
    
    - **SOP 1 (区间再现)：** $\int_0^{\pi/2} \frac{f(\sin x)}{f(\sin x)+f(\cos x)} dx$
        
        - **动作：** 令 $t = \pi/2 - x$，得到 $I = \int_0^{\pi/2} \frac{f(\cos t)}{f(\cos t)+f(\sin t)} dt$。
            
        - $I+I = \int_0^{\pi/2} 1 dx = \pi/2 \implies I = \pi/4$。
            
    - **SOP 2 (华里士公式)：** $\int_0^{\pi/2} \sin^n x dx = \int_0^{\pi/2} \cos^n x dx = \begin{cases} \frac{(n-1)!!}{n!!} \frac{\pi}{2} & n \text{为偶} \\ \frac{(n-1)!!}{n!!} & n \text{为奇} \end{cases}$。 (竞赛中必须能快速写出)
        
    - **SOP 3 (区间 $[0, \pi]$)：** $\int_0^\pi x f(\sin x) dx = \frac{\pi}{2} \int_0^\pi f(\sin x) dx = \pi \int_0^{\pi/2} f(\sin x) dx$。
        

---

##### 模块五：无穷级数 (微积分的终局)

1. **Abel 定理 (收敛域的核心)**
    
    - **SOP：**
        
        1. $\sum a_n (x-x_0)^n$ 在 $x_1$ 处**收敛** $\implies$ 在 $|x-x_0| < |x_1-x_0|$ 内**绝对收敛**。
            
        2. $\sum a_n (x-x_0)^n$ 在 $x_2$ 处**发散** $\implies$ 在 $|x-x_0| > |x_2-x_0|$ 处**发散**。
            
        3. **“条件收敛”** 只能发生在**端点**上。
            
    - **案例 (`S-F-6`):** $\sum a_n(2x-1)^n$ 在 $x=-1$ 条件收敛，求 $\sum a_n x^{2n}$ 的 $R$。
        
        - **思维：** (1) $y=2x-1$，中心 $x_0=1/2$。
            
        - (2) $x=-1$ 是端点 $\implies R_y = |-1 - 1/2| = 3/2$。
            
        - (3) $\sum a_n y^n$ 的 $R_y = 3/2$。
            
        - (4) $\sum a_n x^{2n}$ $\to$ 令 $z=x^2$ $\to \sum a_n z^n$。
            
        - (5) $R_z = R_y = 3/2 \implies |z| < 3/2 \implies |x^2| < 3/2 \implies R_x = \sqrt{3/2}$。
            
2. **函数 $\leftrightarrow$ 级数 (求和与展开)**
    
    - **SOP 1 (反求导数)：** $f^{(n)}(a)$ $\implies$ **立即**在 $x=a$ 处泰勒展开 $f(x)$，对比 $\frac{f^{(n)}(a)}{n!}$。
        
    - **SOP 2 (凑 $e^x$)：** 看到 $\frac{1}{n!}$ $\implies$ 凑 $e^x = \sum \frac{x^n}{n!}$。
        
        - **技巧：** $n^2 = n(n-1)+n$ $\to$ $\frac{n^2}{n!} = \frac{1}{(n-2)!} + \frac{1}{(n-1)!}$。
            
    - **SOP 3 (逐项积分/微分)：** 看到 $\frac{1}{n}$ 或 $n$ $\implies$ **先求导/积分**，变为 $\sum x^n = \frac{1}{1-x}$，**再积/导回去**，最后**代入 $x=0$ 定常数 $C$**。
        
    - **SOP 4 (凑 $\cosh / \cos$)：** 看到“稀疏项” (如 $\frac{x^{4n}}{(4n)!}$)
        
        - **思维：** $\frac{1}{2}(\cosh x + \cos x) = \sum_{n=0}^\infty \frac{x^{4n}}{(4n)!}$。
            
        - $\frac{1}{2}(\sinh x + \sin x) = \sum_{n=0}^\infty \frac{x^{4n+1}}{(4n+1)!}$。
            
3. **傅里叶级数 (Dirichlet 定理)**
    
    - **SOP：**
        
        1. **查奇偶：** $f(x)$ 是偶 $\implies b_n=0$； $f(x)$ 是奇 $\implies a_n=0$。
            
        2. **求系数：** $a_n = \frac{2}{L} \int_0^L \dots$ (必用分部积分)。
            
        3. **求 $\sum \frac{1}{n^2}$ (反求)：**
            
            - 写出 $f(x) = \frac{a_0}{2} + \sum \dots$
                
            - **代入 $x$ 值：** 选一个**连续点** $x_0$ (通常是 $x=0$ 或 $x=L$)。
                
            - $f(x_0) = S(x_0)$，解出级数和。
                
            - **S-T-S 技巧：** $S_{\text{all}} = S_{\text{odd}} + S_{\text{even}}$。
                
            - $S_{\text{even}} = \sum \frac{1}{(2k)^2} = \frac{1}{4} \sum \frac{1}{k^2} = \frac{1}{4} S_{\text{all}}$。
                
            - $S_{\text{all}} = S_{\text{odd}} + \frac{1}{4} S_{\text{all}} \implies S_{\text{all}} = \frac{4}{3} S_{\text{odd}}$。
                
            - (如 `S-J-34` 中 $S_{\text{odd}} = \pi^2/8 \implies S_{\text{all}} = \frac{4}{3}(\frac{\pi^2}{8}) = \frac{\pi^2}{6}$)
                

---

给竞赛选手的最后建议：

竞赛的本质是**“识别”**。你看到一道题，不是在想“我该用什么”，而是在想“这道题的出题人想考我哪个SOP？”。

- 看到 $\theta \to \frac{1}{2}$，就是**泰勒**。
    
- 看到 $f'(\xi) = f(\xi)$，就是**积分因子**。
    
- 看到 $\int_0^\infty x^n e^{-kx} dx$，就是 **$\Gamma$ 函数**。
    
- 看到 $\int f^2$ 和 $(\int f)^2$，就是**柯西**。
    

祝你明天竞赛顺利，拿下高分！